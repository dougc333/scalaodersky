 import FunSets._
 

object f {;import org.scalaide.worksheet.runtime.library.WorksheetSupport._; def main(args: Array[String])=$execute{;$skip(75); 
  println("Welcome to the Scala worksheet");$skip(29); 
 
  val s1 = singletonSet(1);System.out.println("""s1  : <error> = """ + $show(s1 ))}
}
