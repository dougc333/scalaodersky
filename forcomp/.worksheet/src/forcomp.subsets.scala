
package forcomp

import common._

object subsets {;import org.scalaide.worksheet.runtime.library.WorksheetSupport._; def main(args: Array[String])=$execute{;$skip(95); 
  println("Welcome to the Scala worksheet");$skip(36); 
  val ab = List(('a', 2), ('b', 2));System.out.println("""ab  : List[(Char, Int)] = """ + $show(ab ))}
//  val b = wordOccurences("asdf")
  
}
